# === Build outputs (never committed) ===
dist/

# === Dependencies ===
node_modules/

# === Generated diagnostics/reports (never committed) ===
generated/*.json

# === OS junk ===
.DS_Store
__MACOSX/
Thumbs.db

# === Package artifacts ===
*.tgz

/dist/

# === Notes on COMMITTED generated artifacts ===
# assets/manifest.json       — GENERATED by pnpm assets, committed for zero-setup dev
# assets/audio/sprites/      — GENERATED by pnpm assets, committed for zero-setup dev
# src/Asset.d.ts             — GENERATED by pnpm assets, committed for DX
#
# These are generated artifacts that are intentionally committed so that
# `pnpm dev` works immediately after clone+install. They are regenerated
# by the pipeline (pnpm assets).
#
# See docs/ASSET_PIPELINE.md for the full artifact policy.
