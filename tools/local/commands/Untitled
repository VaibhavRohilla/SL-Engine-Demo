# SECTION A — HUD-BOARD-SCREENSHOT-PROOF-BATCH Implementation Prompt
You are working in the SL-Engine repository as a Staff+ slot engine architect, visual QA owner, and hostile production auditor.
Goal:
Implement HUD-BOARD-SCREENSHOT-PROOF-BATCH.
This batch converts the current classic board **probe proof** into real **portrait/landscape screenshot proof**.
Current status:
- `HUD-BOARD-VISUAL-PROOF-DEMO-BATCH` is accepted as **classic probe-level proof**.
- `classicBoardVisualProofConfig` exists and is opt-in.
- Doctor validation for the board proof config passes.
- Probe proof validates portrait/landscape placements, dynamic text, panel buttons, and no duplicate children.
- No screenshot baselines exist yet.
- Cleopatra proof is deferred and must remain separate.
This batch must not add new board runtime features. It is strictly visual regression proof for the existing classic board demo.
Do not add:
- new board element types
- new anchors
- reelWindow/frame anchors
- Spine
- animatedSprite
- bitmapText
- panels
- runtimeAction
- callbacks
- expression parsing
- commercial control migration
- SpinFlow changes
- App renderer/DPR changes
- reel lifecycle changes
- Cleopatra changes
---
# 1. Root problem
The board system now has runtime, doctor, probe, and generated starter proof.
But probe proof is not screenshot proof.
A studio/client needs visual confidence:
- Does the board actually look good?
- Does portrait still look clean?
- Does landscape still look clean?
- Are logo/title/buttons/text visible?
- Are elements not overlapping existing commercial HUD controls?
- Are screenshots stable enough for CI?
This batch must create a repeatable screenshot proof command for the opt-in classic board demo.
---
# 2. Target proof
Add screenshot proof for the generated **classic starter** with `classicBoardVisualProofConfig` enabled.
Required captures:
```text
classic board visual proof — landscape
classic board visual proof — portrait

Recommended viewport sizes:

landscape: 1280x720
portrait: 390x844 or 720x1280

Use the same dimensions already used in existing starter/browser smoke tests if there is a standard.

⸻

3. Activation strategy

The default starter must remain unchanged.

Do not enable the board proof config in normal starter output unless explicitly requested by the screenshot test.

Preferred options:

Option A — query flag

Enable proof config with a query param:

?slBoardVisualProof=1

or:

?slTest=1&slBoardVisualProof=1

Option B — test-only generated patch

The screenshot test generates the classic starter, then patches/imports classicBoardVisualProofConfig into the generated HUD config.

Option C — environment flag

Use env only if the starter already has an env-driven test pattern.

Preferred:
Use the least invasive option that matches existing generated starter test patterns.

Rules:

* do not change default visual output
* do not add runtime hacks
* do not make board proof config active for real consumers by accident

⸻

4. Screenshot infrastructure

Search existing repo for screenshot/Playwright visual tests.

If existing screenshot infra exists:

* reuse it
* follow current naming/output patterns
* follow current threshold conventions

If not:

* add minimal screenshot proof infrastructure under starter tests
* keep it isolated
* do not introduce heavy new dependencies if Playwright already exists

Suggested paths:

tests/starter/hud-board-screenshot-proof.test.ts
scripts/test-hud-board-screenshot-proof.ts

or follow existing generated starter test script conventions.

Add package script:

"test:hud-board-screenshot-proof": "..."

⸻

5. Baseline storage

Choose a deterministic baseline location.

Suggested:

tests/visual-baselines/hud-board/classic-landscape.png
tests/visual-baselines/hud-board/classic-portrait.png

or use existing repo snapshot convention.

Also store latest actual/diff output in ignored artifact folder if existing.

Rules:

* commit baselines if repo convention allows it
* do not commit random generated artifacts
* do not rely on local absolute paths
* avoid OS-specific screenshot naming

⸻

6. Screenshot stability

Make screenshots deterministic.

Before capture:

* wait for engine ready
* wait for board root mounted
* wait for known board elements visible
* set deterministic balance/win state if possible
* avoid animation-sensitive timing
* disable or freeze nonessential animations if existing test mode supports it
* wait for one stable render frame after state update
* use deterministic viewport DPR if existing browser smoke controls it

Do not add global rendering hacks.

If existing slot reels animate in background:

* crop screenshot to HUD/board area if useful
* or capture full canvas only after idle
* or add a mask/threshold that tolerates reel content if baseline infrastructure supports it

Preferred:
Use a stable test mode with deterministic scene state.

⸻

7. Visual assertions before screenshot compare

Before comparing screenshots, assert:

* board root exists
* expected element labels exist
* brand/title visible
* buy button visible
* balance text visible
* win text visible when expected
* settings/sound/paytable buttons visible
* elements are inside viewport
* no expected element has zero bounds
* portrait positions differ from landscape positions
* doctor reports zero errors for proof config

This prevents screenshot diff from being the only failure signal.

⸻

8. Screenshot comparison policy

Define threshold honestly.

Preferred:

* strict enough to catch layout regressions
* tolerant enough for minor canvas/platform antialiasing

Document:

* exact diff threshold
* what is compared
* where baselines live
* how to update baselines intentionally

If the repo already has visual threshold standards, use them.

Do not claim pixel-perfect if the threshold is tolerant.

⸻

9. Dynamic text proof in screenshot

The screenshot proof should include visible dynamic values.

Recommended deterministic values:

balance: $12,345.00
win: $2,500.00

If the test harness can set board bindings:

* set values before screenshot
* verify rendered text via probe before capture

If not:

* use current default binding values
* document limitation
* keep dynamic text proof in existing probe test

Preferred:
Set deterministic board state.

⸻

10. Doctor integration

Before screenshot:

* run collectHudBoardDiagnostics for the proof config
* assert zero error diagnostics
* ideally zero warnings
* if warnings are expected, assert exact warning codes

Do not ignore doctor warnings silently.

⸻

11. Reports and docs

Update:

docs/reference/HUD_CONFIGURATION.md
docs/TESTING_AND_DOCTOR.md
docs/reference/COMMERCIAL_HUD_CONTRACT.md

Add:

* how to run screenshot proof
* how to update baselines
* proof config remains opt-in
* screenshot proof covers classic starter only
* Cleopatra proof is deferred
* screenshot threshold and limitations

Create:

docs/internal/HUD_BOARD_SCREENSHOT_PROOF_BATCH_REPORT.md

Include:

1. Executive verdict
2. Files changed
3. Activation strategy
4. Screenshot baseline locations
5. Landscape proof
6. Portrait proof
7. Doctor result
8. Commands run
9. Remaining risks
10. Next recommended batch

⸻

12. Commands

Run:

pnpm typecheck
pnpm vitest run tests/slot/ui/hud
pnpm vitest run tests/governance
pnpm test:hud-board-overlap-hit
pnpm test:hud-board-visual-proof
pnpm test:hud-board-screenshot-proof
pnpm test:generated -- --starter classic
pnpm test:generated -- --starter gravity

Run full:

pnpm test

if practical.

If generated starter commands timeout due known SDK packaging flake:

* rerun sequentially once
* document exact first failure and rerun result
* do not hide persistent failures

⸻

13. Acceptance criteria

PASS only if:

* classic board screenshot proof command exists
* landscape screenshot baseline exists
* portrait screenshot baseline exists
* screenshot comparison passes
* board proof config remains opt-in
* doctor passes for proof config
* board root/elements are asserted before screenshot
* generated classic passes
* generated gravity passes
* overlap hit probe still passes
* no unsupported runtime features added
* no App/SpinFlow/reel/commercial-shell scope creep
* report exists and is honest

If screenshots cannot be made stable:

* keep verdict PARTIAL
* keep probe proof accepted
* document screenshot blocker precisely
* do not fake screenshot pass

Final response:

1. Executive verdict
2. Files changed
3. Activation strategy
4. Screenshot baselines
5. Visual assertions
6. Doctor results
7. Tests run
8. Remaining risks
9. Next recommended batch

```markdown
# SECTION B — HUD-BOARD-SCREENSHOT-PROOF-BATCH Hostile Audit Prompt
You are auditing HUD-BOARD-SCREENSHOT-PROOF-BATCH as a hostile Staff+ slot engine architect and CTO.
Final verdict must be PASS, PARTIAL, or FAIL.
This batch should add screenshot regression proof for the opt-in classic board visual proof config.
It must not add new board runtime features, new anchors, renderer types, callbacks, expressions, panel rewrites, or commercial shell migration.
---
# 1. Scope
Verify the batch only adds:
- screenshot/proof test
- baseline images if repo convention allows
- activation path for opt-in classic board proof
- docs
- report
- minimal test harness support
FAIL if it adds:
- Spine
- animatedSprite
- bitmapText
- new anchors
- panels
- runtimeAction
- callbacks
- expression parser
- SpinFlow/App changes
- commercial control migration
---
# 2. Opt-in behavior
Verify:
- default classic starter remains unchanged
- board proof config is enabled only for screenshot/proof command
- no accidental default visual change
FAIL if normal starter now shows proof board unintentionally.
---
# 3. Screenshot proof
Verify:
- landscape screenshot baseline exists
- portrait screenshot baseline exists
- comparison runs in CI/test command
- threshold is documented
- actual/diff outputs are handled correctly
- proof waits for engine/board readiness before capture
PARTIAL if screenshots are generated but not compared.
FAIL if screenshot command is fake or only checks DOM labels.
---
# 4. Visual assertions
Verify screenshot test also asserts:
- board root exists
- expected board elements exist
- elements visible
- non-zero bounds
- inside viewport/safeArea
- portrait/landscape positions differ
- no forbidden overlap with critical HUD controls
FAIL if it only takes screenshots without semantic assertions.
---
# 5. Doctor proof
Verify:
- proof config passes board doctor
- zero errors
- warnings either zero or explicitly expected
- missing asset/textKey negative coverage remains elsewhere
FAIL if screenshot proof runs with doctor errors.
---
# 6. Dynamic text proof
Verify:
- balance/win text visible or explicitly probed
- values deterministic if possible
- screenshots capture stable state
- no stale layout/hit geometry regression
PARTIAL if dynamic value screenshot is not deterministic but probe proof remains.
---
# 7. Regression commands
Verify:
```bash
pnpm typecheck
pnpm vitest run tests/slot/ui/hud
pnpm vitest run tests/governance
pnpm test:hud-board-overlap-hit
pnpm test:hud-board-visual-proof
pnpm test:hud-board-screenshot-proof
pnpm test:generated -- --starter classic
pnpm test:generated -- --starter gravity

PASS requires generated starters green at least once.

⸻

8. Report honesty

Open:

docs/internal/HUD_BOARD_SCREENSHOT_PROOF_BATCH_REPORT.md

Verify it states:

* screenshot vs probe proof clearly
* baseline paths
* threshold
* commands run
* limitations
* Cleopatra deferred if not included
* next batch

FAIL if it overclaims Cleopatra or pixel-perfect proof.

⸻

Verdict rules

PASS:

* screenshot baselines exist
* portrait/landscape screenshot comparison passes
* doctor passes
* visual assertions pass
* generated starters pass
* no scope creep

PARTIAL:

* screenshot infra exists but unstable
* screenshots captured but baseline comparison deferred honestly
* only probe proof remains

FAIL:

* no real screenshots
* demo invalid
* default starter changed unintentionally
* unsupported runtime features added
* tests fail
* report overclaims

Output:

1. Executive verdict
2. Evidence table
3. Files inspected
4. Commands run
5. Blockers
6. Required fixes
7. Final recommendation

